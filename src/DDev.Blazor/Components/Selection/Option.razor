<li class="menu-item" tabindex="@(Disabled ? -1 : 0)" disabled="@Disabled" @onclick="HandleClick">
    <div class="menu-item__icon"><Icon Name="@Icon" /></div>
    <div class="menu-item__label">@ChildContent</div>
    <div class="menu-item__hint">
        @Hint
        <Icon Name="@HintIcon" />
    </div>
</li>